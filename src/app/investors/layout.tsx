import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Invest in CTekk Solutions | AI Workforce Infrastructure',
  description: 'CTekk Solutions investor relations. Services-to-SaaS growth engine across AI talent, enterprise training, and workforce platforms. Revenue-generating from day one.',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
